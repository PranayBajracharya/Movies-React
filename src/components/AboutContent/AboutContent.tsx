import {
    GridItem,
    Heading,
    HStack,
    VStack,
    Text,
    Image,
} from "@chakra-ui/react";

const AboutContent: React.FC = () => {
    return (
        <HStack>

        </HStack>
    );
};

export default AboutContent;